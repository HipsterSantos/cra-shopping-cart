module.exports = function Cart(odlItem){
    this.oldItem = oldItem;
    this.oldPrice = oldItem.price; 
    this.oldAmount = oldItem.amount;
    this.totalPrice = 0;
    this.totalAmount = 0;

    this.add  = function(){
        
    }
}